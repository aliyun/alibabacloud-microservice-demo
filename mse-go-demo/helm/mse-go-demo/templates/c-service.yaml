apiVersion: v1
kind: Service
metadata:
  name: go-c-service
  namespace: default
spec:
  ports:
    - name: port-1
      port: 50051
      protocol: TCP
      targetPort: 50051
  selector:
    app: c-server
  type: ClusterIP